# CSM admin password for Edit/Save
CSM_ADMIN_PASSWORD=klaviyo-csm

# Durable plan Save on Vercel (Project → Storage → Create Blob Store).
# BLOB_READ_WRITE_TOKEN=

# Do NOT use the customer’s KLAVIYO_PRIVATE_API_KEY.
# Refresh metrics via Cursor + Klaviyo MCP (SSO).
# Optional later: Klaviyo OAuth for in-app Refresh.
