# CSM admin password for Edit/Save (+ metrics ingest)
CSM_ADMIN_PASSWORD=klaviyo-csm

# Optional dedicated token for Claude/Cursor metrics ingest
# (x-metrics-ingest-token or Authorization: Bearer)
# METRICS_INGEST_TOKEN=

# Durable plan Save on Vercel (Project → Storage → Create Blob Store).
# BLOB_READ_WRITE_TOKEN=

# Experiment metrics are refreshed via Klaviyo MCP (SSO) → metrics ingest.
# Do not require KLAVIYO_PRIVATE_API_KEY for the normal workflow.
