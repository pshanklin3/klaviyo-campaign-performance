# Klaviyo private API key (Settings → API keys)
# Required scopes: campaigns:read, metrics:read
KLAVIYO_PRIVATE_API_KEY=

# Optional: Placed Order (or other conversion) metric ID
# If omitted, the app looks up "Placed Order" automatically
KLAVIYO_CONVERSION_METRIC_ID=
